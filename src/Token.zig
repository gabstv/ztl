const lib = @import("lib.zig");

const Position = lib.Position;

const Token = @This();

value: Value,
position: Position,

pub const Value = union(Type) {
    AND,
    BANG,
    BANG_EQUAL,
    BOOLEAN: bool,
    COMMA,
    DOT,
    ELSE,
    EOF,
    EQUAL,
    EQUAL_EQUAL,
    FLOAT: f64,
    FN,
    GREATER,
    GREATER_EQUAL,
    IDENTIFIER: []const u8,
    IF,
    INTEGER: i64,
    LEFT_BRACE,
    LEFT_BRACKET,
    LEFT_PARENTHESIS,
    LESSER,
    LESSER_EQUAL,
    MINUS,
    NULL,
    OR,
    PLUS,
    RETURN,
    RIGHT_BRACE,
    RIGHT_BRACKET,
    RIGHT_PARENTHESIS,
    SEMICOLON,
    SLASH,
    STAR,
    START,
    STRING: []const u8,
    VAR,
    VOID,
    WHILE,
};

pub const Type = enum {
    AND,
    BANG,
    BANG_EQUAL,
    BOOLEAN,
    COMMA,
    DOT,
    ELSE,
    EOF,
    EQUAL,
    EQUAL_EQUAL,
    FLOAT,
    FN,
    GREATER,
    GREATER_EQUAL,
    IDENTIFIER,
    IF,
    INTEGER,
    LEFT_BRACE,
    LEFT_BRACKET,
    LEFT_PARENTHESIS,
    LESSER,
    LESSER_EQUAL,
    MINUS,
    NULL,
    OR,
    PLUS,
    RETURN,
    RIGHT_BRACE,
    RIGHT_BRACKET,
    RIGHT_PARENTHESIS,
    SEMICOLON,
    SLASH,
    STAR,
    START,
    STRING,
    VAR,
    VOID,
    WHILE,
};
